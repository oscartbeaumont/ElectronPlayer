appId: xyz.simp-bot.ElectronPlayer
artifactName: ${name}-v${version}_${os}-${arch}.${ext}
mac:
  category: public.app-category.entertainment
win:
  target:
    - msi
    - portable
snap:
  plugs:
    - default
    - screen-inhibit-control
linux:
  category: Video
  target:
    - AppImage
    - snap
    - deb
  icon: build/512x512.png
  
electronDownload:
  mirror: https://github.com/castlabs/electron-releases/releases/download/
  
